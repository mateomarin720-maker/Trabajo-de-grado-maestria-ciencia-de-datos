# data/2_models

Objetos serializados de estimadores y resultados intermedios: modelos
LASSO/Ridge y SHAP (OE1), modelos de suficiencia explicativa (OE2), y
objetos DML / Causal Forest con su identificación documentada (OE3),
producidos por `src/4_dag_identificacion.py`, `src/5_estimacion.py` y
`src/6_refutacion.py`.

Contenido no versionado por `.gitignore` (excepto este archivo).
